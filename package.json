{
  "name": "ghumuam-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run preview --prefix frontend"
  }
}
